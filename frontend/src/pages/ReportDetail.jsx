// ReportDetail.jsx — same layout as Dashboard, but for historical uploads
// Loads by :uploadId from URL params

import Dashboard from './Dashboard.jsx'

// ReportDetail is functionally identical to Dashboard — both load by uploadId param.
// This component simply re-exports Dashboard to satisfy the routing distinction.
export default Dashboard
